apiVersion: v1
kind: Service
metadata:
  name: apache-service
  namespace: dev-ns

spec:
  selector:
    app: apache-pod

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

  type: ClusterIP