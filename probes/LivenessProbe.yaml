# Namespace for isolating resources
apiVersion: v1
kind: Namespace
metadata:
  name: dev-ns

---

# NGINX Pod
apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  namespace: dev-ns

spec:
  containers:
    - name: nginx-container
      image: nginx:latest

      ports:
        - containerPort: 80 # Expose container on port 80

      livenessProbe:
        httpGet:
          path: / # Send HTTP request to "/"
          port: 80 # Check health on port 80

        initialDelaySeconds: 5 # Wait before first probe
        periodSeconds: 4 # Run probe every 4 seconds
        timeoutSeconds: 3 # Probe timeout after 3 seconds
        failureThreshold: 4 # Restart after 4 failed probes